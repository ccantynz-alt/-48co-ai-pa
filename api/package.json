{
  "name": "48co-api",
  "version": "1.0.0",
  "description": "48co managed API proxy — handles Claude and Whisper API calls so users never need their own keys.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.18.0",
    "cors": "^2.8.5",
    "helmet": "^7.1.0",
    "express-rate-limit": "^7.1.0",
    "better-sqlite3": "^11.0.0",
    "nanoid": "^5.0.0",
    "bcrypt": "^5.1.0"
  }
}
