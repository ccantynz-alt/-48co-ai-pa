{
  "name": "48co",
  "version": "1.0.0",
  "description": "Voice-to-text that types into any app. Like WhisperTyping, but better.",
  "main": "main/index.js",
  "scripts": {
    "start": "electron .",
    "dev": "cross-env NODE_ENV=development electron .",
    "icons": "node scripts/generate-icons.js",
    "prebuild:mac": "npm run icons",
    "prebuild:win": "npm run icons",
    "prebuild:all": "npm run icons",
    "build:mac": "electron-builder --mac --publish never",
    "build:win": "electron-builder --win --publish never",
    "build:all": "electron-builder --mac --win --publish never",
    "release": "electron-builder --publish always",
    "postinstall": "electron-builder install-app-deps"
  },
  "build": {
    "appId": "com.48co.voice",
    "productName": "48co",
    "artifactName": "48co-${os}.${ext}",
    "directories": {
      "output": "dist"
    },
    "files": [
      "main/**/*",
      "renderer/**/*",
      "assets/**/*",
      "node_modules/**/*"
    ],
    "mac": {
      "category": "public.app-category.productivity",
      "icon": "assets/icon.png",
      "target": [
        { "target": "dmg", "arch": ["universal"] }
      ],
      "hardenedRuntime": true,
      "entitlements": "assets/entitlements.mac.plist",
      "extendInfo": {
        "NSMicrophoneUsageDescription": "48co needs microphone access for voice-to-text transcription.",
        "NSAccessibilityUsageDescription": "48co needs accessibility access to type text into other applications."
      }
    },
    "dmg": {
      "artifactName": "48co-mac.${ext}"
    },
    "win": {
      "icon": "assets/icon.png",
      "target": [
        { "target": "nsis", "arch": ["x64"] }
      ]
    },
    "nsis": {
      "oneClick": true,
      "perMachine": false,
      "allowToChangeInstallationDirectory": false,
      "artifactName": "48co-win.${ext}"
    },
    "publish": {
      "provider": "github",
      "owner": "ccantynz-alt",
      "repo": "-48co-ai-pa"
    }
  },
  "dependencies": {
    "@nut-tree-fork/nut-js": "^4.2.0",
    "electron-store": "^8.2.0",
    "electron-updater": "^6.1.0",
    "form-data": "^4.0.0",
    "node-fetch": "^3.3.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "electron": "^28.0.0",
    "electron-builder": "^24.9.0",
    "sharp": "^0.33.0"
  }
}
